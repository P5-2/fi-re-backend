CREATE TABLE snsuser (
                         username VARCHAR(200) NOT NULL,
                         password VARCHAR(200),
                         name VARCHAR(200),
                         auth VARCHAR(200),
                         PRIMARY KEY (username)
);
