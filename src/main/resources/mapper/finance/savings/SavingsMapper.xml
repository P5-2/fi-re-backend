<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fi.re.firebackend.dao.finance.savings.SavingsDao">
    <select id="getSavingsById"
            parameterType="int"
            resultType="fi.re.firebackend.dto.finance.savings.SavingsDto">
        SELECT prdNo, pname, type, bname, minRate, maxRate, subPeriod, subAmount, target, benefit, description, selectCount, keyword, InCart
        FROM savings
        where prdNo = #{prdNo}
    </select>

    <select id="hot"
            resultType="fi.re.firebackend.dto.finance.savings.SavingsDto">
        SELECT prdNo, pname, type, bname, minRate, maxRate, subPeriod, subAmount, target, benefit, description, selectCount, keyword. InCart
        FROM savings
        order by selectCount desc
        limit 3
    </select>

    <select id="all"
            resultType="fi.re.firebackend.dto.finance.savings.SavingsDto">
        SELECT prdNo, pname, type, bname, minRate, maxRate, subPeriod, subAmount, target, benefit, description, selectCount, keyword, InCart
        FROM savings
    </select>

    <!-- type에 따른 목록 가져오기 -->
    <select id="selectSavingsByType" parameterType="String" resultType="fi.re.firebackend.dto.finance.savings.SavingsDto">
        SELECT
            prdNo, pname, type, bname, minRate, maxRate, subPeriod,
            subAmount, target, benefit, description, selectCount,
            keyword, InCart
        FROM
            savings
        WHERE
            type = #{type}
    </select>

    <!-- 비교함 추가 및 삭제 -->
    <update id="updateSavingsCartStatus">
        UPDATE savings SET InCart = #{InCart} WHERE prdNo = #{prdNo}
    </update>

    <!-- 비교함 목록 조회 -->
    <select id="selectSavingsInCart" resultType="fi.re.firebackend.dto.finance.savings.SavingsDto">
        SELECT * FROM savings
        WHERE InCart = 1
    </select>

    <!-- 현재 비교함 상태를 가져오는 쿼리 -->
    <select id="getSavingsCartStatus" resultType="int">
        SELECT InCart FROM savings WHERE prdNo = #{prdNo}
    </select>
</mapper>