<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fi.re.firebackend.dao.forex.ForexDao">
    <insert id="insertExchangeRate">
        INSERT INTO Exchange_Rate (searchdate, cur_unit, result, ttb, tts, deal_bas_r, bkpr, yy_efe_r, ten_dd_efe_r, kftc_bkpr, kftc_deal_bas_r)
        VALUES (#{searchDate}, #{curUnit}, #{result}, #{ttb}, #{tts}, #{dealBasR}, #{bkpr}, #{yyEfeeR}, #{tenDdEfeeR}, #{kftcBkpr}, #{kftcDealBasR})
    </insert>

    <select id="selectExchangeRateByDate" resultType="fi.re.firebackend.dto.forex.ForexDto">
        SELECT * FROM Exchange_Rate WHERE searchdate = #{searchDate}
    </select>

</mapper>
