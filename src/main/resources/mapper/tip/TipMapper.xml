<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fi.re.firebackend.dao.tip.TipDao">
    <select id="findAll"
            resultType="fi.re.firebackend.dto.tip.TipDto">
        select id, name, type, path, count
        from tip
    </select>
</mapper>