<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true" />
        <setting name="logImpl" value="LOG4J" />
    </settings>

    <typeAliases>
        <package name="fi.re.firebackend.dto.gold" />
        <package name="fi.re.firebackend.dto.login"/>
        <package name="fi.re.firebackend.dto."/>
        <package name="fi.re.firebackend.jwt.dto"/>
    </typeAliases>

    <mappers>
        <mapper resource="mapper/gold/GoldDao.xml"/>
        <mapper resource="mapper/login/Member.xml"/>
        <mapper resource="mapper/recommendation/DepositDao.xml"/>
        <mapper resource="mapper/recommendation/FundDao.xml"/>
        <mapper resource="mapper/exp/ExpMapper.xml"/>
    </mappers>

</configuration>
